# A2 | class agentic | final T2 | escalations 2 | flagged False
--- QUERY ---
A Python batch script suddenly gets OOM-killed at 3am nightly, only in production. Give an ordered diagnostic checklist cheapest-first.
--- ANSWER ---
Here's a thinking process:

1.  **Understand User Problem:**
   - **Issue:** Python batch script gets OOM-killed (Out of Memory)
   - **Timing:** 3am nightly
   - **Environment:** Only in production (not in dev/staging)
   - **Request:** Ordered diagnostic checklist, cheapest-first

2.  **Key Constraints & Context:**
   - "Cheapest-first" means starting with free, non-invasive, low-cost diagnostic steps before moving to paid tools, infrastructure changes, or complex debugging.
   - "Only in production" suggests differences in data volume, configuration, environment, scheduling, or external dependencies.
   - "3am nightly" suggests it's a scheduled batch job, possibly processing daily data, interacting with databases, or running during low-traffic periods.
   - Python OOM-kills are typically handled by the OS (OOM killer) or the runtime (e.g., Python's memory limits, though Python doesn't have built-in hard limits like some languages).

3.  **Brainstorming Diagnostic Steps (Cheapest-First):**
   - *Free/No-cost:*
     - Check logs (system, application, scheduler)
     - Review recent changes (code, config, data, dependencies)
     - Check resource limits (cgroups, ulimits, systemd limits)
     - Verify data volume/size differences between envs
     - Check for memory leaks (basic profiling)
     - Review external dependencies (DB connections, APIs, caches)
     - Check for zombie processes or orphaned workers
     - Verify Python version/interpreter differences
     - Check for large object accumulation (lists, dicts, caches)
     - Review garbage collection behavior
   - *Low-cost/Minimal:*
     - Use built-in Python memory profilers (tracemalloc, objgraph)
     - Check `free -m`, `top`, `htop`, `vmstat`
     - Review scheduler logs (cron, Airflow, etc.)
     - Check for duplicate runs or overlapping jobs
   - *Higher-cost/Advanced:* (Avoid unless necessary, but keep in mind)
     - Paid APM tools
     - Infrastructure scaling
     - Deep debugging with perf, valgrind, etc.

4.  **Structure the Checklist (Ordered, Cheapest-First):**
   I'll organize it logically from quick/free checks to slightly more involved but still low-cost steps, ensuring each step is actionable and explains *why* it's relevant to the specific symptoms (3am, production-only, Python OOM).

   **Phase 1: Zero-Cost / Immediate Checks**
   1. Verify the exact OOM killer message & process
   2. Check for duplicate/overlapping job executions
   3. Compare production vs. dev/staging data volume & config
   4. Review recent deployments/changes (code, deps, env vars)
   5. Check system resource limits (cgroups, ulimits, systemd)
   6. Inspect scheduler logs for anomalies (retry loops, backpressure)

   **Phase 2: Low-Cost / Built-in Diagnostics**
   7. Run basic memory profiling (tracemalloc, objgraph)
   8. Check for connection/cache leaks (DB, Redis, file handles)
   9. Verify Python interpreter & extension differences
   10. Monitor memory growth pattern (pre-OOM)

   **Phase 3: Targeted Fixes / Validation**
   11. Implement explicit memory management (del, gc, chunking)
   12. Add graceful degradation / memory limits
   13. Validate with production-like data in staging

   Wait, the user asked for a *diagnostic checklist*, not a fix checklist. I should focus on diagnosis, but can include validation steps. I'll keep it strictly diagnostic, ordered by cost/effort.

   Let's refine and order properly:

   **1. Confirm the OOM killer & process** (Free: `dmesg | grep -i oom`, `journalctl`)
   **2. Check for overlapping/duplicate runs** (Free: scheduler logs, `ps aux`, lock files)
   **3. Compare data volume & schema between environments** (Free: DB stats, file sizes, config diff)
   **4. Review recent changes (code, deps, env vars, Python version)** (Free: git log, pip freeze diff, env comparison)
   **5. Check system-level resource limits** (Free: `cat /proc/self/cgroup`, `ulimit -a`, systemd `LimitNOFILE`, `MemoryMax`)
   **6. Inspect scheduler/job orchestration behavior** (Free: cron/Airflow/Argo logs, retry policies, backoff)
   **7. Run built-in Python memory profiling** (Free: `tracemalloc`, `objgraph`, `pymem` if available)
   **8. Check for connection/cache/file handle leaks** (Free: `lsof`, DB pool stats, `gc.get_referrers()`)
   **9. Verify interpreter & extension differences** (Free: `python --version`, `ldd`, C-extension memory behavior)
   **10. Capture pre-OOM memory growth pattern** (Free: `ps aux --sort=-%mem`, `vmstat`, periodic `tracemalloc` snapshots)

   This is good, but I need to make it more structured, actionable, and explicitly tie each step to the "3am production-only" context. I'll add brief explanations and commands for each.

   Let's organize it into a clear, ordered checklist with:
   - Step number
   - Action
   - Why it matters (especially for 3am/prod-only)
   - Commands/Tools (free/cheapest)
   - Expected outcome

   I'll also add a "stop early" note: if any step reveals the root cause, stop.

   **Refined Structure:**
   1. **Confirm OOM killer & exact process** → `dmesg | grep -i oom`, `journalctl -k`
   2. **Check for duplicate/overlapping executions** → Scheduler logs, `ps`, lock files
   3. **Compare production vs. non-prod data/config** → DB row counts, file sizes, env vars
   4. **Audit recent changes** → Git diff, pip freeze, Python version, C-extensions
   5. **Verify system resource limits** → cgroups, ulimits, systemd limits
   6. **Inspect scheduler/job behavior** → Retry loops, backpressure, queue buildup
   7. **Run built-in Python memory profiling** → `tracemalloc`, `objgraph`
   8. **Check for connection/cache/file handle leaks** → `lsof`, DB pool, `gc`
   9. **Capture memory growth timeline** → `ps`, `vmstat`, periodic snapshots
   10. **Validate interpreter/extension differences** → `ldd`, Python build flags, numpy/pandas versions

   Wait, some of these
